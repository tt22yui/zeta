# Zeta 功能计划

> ✅ 完成　❌ 未完成

## 预览
- 空格预览文件（图片/视频/音频/PDF/文本，右侧抽屉） ✅

## 右键菜单（文件操作）
- 打开文件 ✅
- 删除 ✅
- 复制文件名 ✅
- 复制路径 ✅
- 收入文件夹（新建文件夹收纳选中项） ✅
- 解散文件夹（子项上移 + 删空壳） ✅
- 复制文件（需 CF_HDROP 文件级剪贴板） ❌
- 剪切文件 ❌

## 撤销 / 删除（后端已就绪，待接入 UI）
- 删除到回收站（`Delete` 键 / 右键菜单项；后端 `delete_file` 已注册） ❌
- 撤销 / 重做（`Ctrl+Z` / `Ctrl+Shift+Z`；后端 `undo` / `redo` 与 `History` 栈已就绪） ❌

## 浏览 / 导航
- 多标签浏览 ❌
- 地址栏自适应省略（中段省略可点击回跳） ❌
- 驱动器按钮 macOS 支持（枚举 /Volumes） ❌

## 设置
- 设置面板（主题三态 / 历史 / 标签分隔符） ✅

## 动效
- 解散文件夹动画（收缩淡出 + reduced-motion 兜底） ✅