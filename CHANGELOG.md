# Changelog

本项目的所有重要变更都会记录在此文件中。

## [v0.1.6] - 2026-09-11

### 新特性

- 设置面板：修改标签分隔符前增加二次确认，避免误改导致旧标签不再被识别。
- 内部拖放：把条目拖到列表中的文件夹行上松手即执行「剪切」移动到该文件夹，
  悬停时高亮落点，同名项自动加序号保留双方；只会移动当前目录内的条目（从系统拖进来的外部文件不受影响）。

### 修复

- 撤销 / 重做失败不再丢失历史条目；实现改为「出栈 → 执行 → 失败放回」，同时消除并发下重复应用同一条目。
- 解散文件夹 / 收入文件夹中途失败会回滚，不再留下「移动了一半 + 空壳残留」且无法撤销的目录。
- 选中文件不再导致文件监听反复重建；网络路径（UNC）的轮询节拍与「连续超时」提示恢复正常。
- 预览面板不再随自动刷新重复读取 1 MiB 文本并重新解析 Markdown（网络路径下原本每 3 秒一次）。
- 修复 Markdown 预览中的链接会把整个 Webview 导航走且无法返回。
- 修复 5 处「延迟注册的外部点击监听」未取消导致的监听器泄漏（弹层反复开关会持续累积）。
- 修复超时包装把「失败」折叠成「超时」，网络路径的权限 / 路径错误不再被误报为响应超时。
- 修复地址栏历史被写坏时列表崩屏；复制失败与打开链接失败不再静默。
- 修复以 `//server/share` 写法访问的网络路径未按 UNC 处理。
- 修复目录列举中单个条目出错（如 macOS 上的悬空软链）导致整个目录列不出来。
- 修复打标签会写入重复标签（`a#x#x`）以及含非法字符的标签直接抛出系统级错误。
- 修复启动时盘符加载把「成功返回空」当失败重试（macOS 上白等 3 秒并多发 5 次 IPC）。
- 修复 macOS 上从一级目录（如 `/Users`、`/Applications`）无法回退到根目录（「上一级」按钮被禁用）。
- 收藏菜单的删除按钮此前嵌套在按钮内部、键盘不可达，改为两个独立的可聚焦菜单项。
- 新增顶层错误边界：渲染异常不再白屏。

### 性能

- 抽出 memo 化的行组件：点击、移动光标、搜索或标签输入、通知与弹层开关不再重渲染整表。
- 搜索过滤改用 `useDeferredValue`；标签色与未知扩展名样式加缓存；翻页步长按实测行高计算。
- 行内重命名改为非受控输入（此前每敲一个字符都会重渲染整个列表）。
- 拖拽图标与 Markdown 解析器按需缓存。

### 可访问性

- 列表补 `listbox` / `option` 语义；收藏、盘符、面包屑菜单支持 ↑↓/Home/End/Enter/Esc 导航。
- 空格不再与按钮、菜单项冲突；弹层或对话框打开时裸 ←→ 不再操作背后的列表。
- 主题单选组支持方向键；类型图标 `aria-hidden`；右键菜单加速键按平台显示 `⌘`。

### 工程改进

- 打包：Windows 不再生成任何安装包（含 NSIS / MSI），由 `tauri.windows.conf.json` 的 `bundle.active = false` 保证；
  macOS 仅产出 dmg；基础配置 targets 改为显式 `["app", "dmg"]`。
- CI：新增发布前校验（前端类型检查 + 前端构建 + `cargo test`）并作为发布硬闸门；移除失效的 `lfs` 参数。
- `Cargo.toml` 补 `license` / `repository`；capabilities 删除无调用的 `core:window:allow-unmaximize`。
- 单元测试 28 → 46（撤销栈语义、失败回滚、标签清洗、UNC 判定等）。
- AGENTS.md 修正前端验证命令（`npm run tsc` 会报 Missing script）与设计令牌说明。

### 文档

- README（中英）：删除界面尚未接入的「撤销 / 重做」「`Delete` 删除」声明；
  补齐空格预览、文件夹收纳、地址栏即导航、设置面板、拖拽到外部应用等功能清单；
  注明 macOS 仅产出 Apple Silicon 包。

## [v0.1.5] - 2026-09-09

### 新特性

- 预览面板增强：Markdown 富文本、音频频谱、自动播放与格式兜底。
- 键盘选中文件时，空格预览跟随光标联动。
- 支持把文件原生拖拽到外部应用（使用真实文件句柄，飞书/企微等可接收）。
- 导航：地址栏小工具与长路径自适应省略；收藏路径、网络图标、F2 重命名与弹层收敛。
- 界面：解散文件夹动画、标签输入快捷按钮；程序提示统一为多级 Toaster，标签栏改平铺展示。
- 应用界面更名「标签匣」（产物名保持 Zeta）。

### 修复

- 修复 Windows 上以点开头的普通文件被当作隐藏项过滤。
- 修复上退多层后仍可用 `→` 跨层重入子目录。

### 工程改进

- 移除 Git LFS，恢复为普通 git 存储。
- 样式：调整标签输入框的宽度与过渡动画。

### 文档

- 新增英文 README 并互加语言切换链接；新增三项计划（解散动画 / 地址栏省略 / 驱动器 macOS）。

## [v0.1.4] - 2026-09-04

### 变更

- 移除 `Delete` 键删除、右键菜单删除项与 `Alt+←/→` 历史前进后退快捷键。
  删除与撤销/重做自此**不再有界面入口**，仅保留后端命令与撤销栈（见 PLAN.md）。

### 修复

- 修复启动早期盘符下拉不可用。
- 返回上一级后保持子目录选中。

### 工程改进

- CI：macOS 构建产出 Apple Silicon（aarch64）包。

## [v0.1.3] - 2026-09-04

### 新特性

- 新增设置面板：主题三态、地址栏历史条数、启动恢复上次路径、可配置标签分隔符。
- 新增空格预览面板、解散文件夹、收入文件夹，并统一确认/输入对话框。
- 前进后退改为全局快捷键，重构方向键选中语义。

### 修复

- 路径失效时自动回退到父目录或默认目录；网络路径轮询增加超时保护，避免界面卡死。

### 工程改进

- CI：打 tag 直接发布 Release，不再创建草稿。

### 文档

- 提交信息一律使用中文撰写。

## [v0.1.2] - 2026-09-02

### 工程改进

- GitHub Actions 发布工作流增加 npm 依赖缓存（`setup-node` 配置 `cache: npm`），复用 `package-lock.json` 缓存 `node_modules`，缩短 CI 安装耗时。

### 文档

- AGENTS.md 新增「发布日志」章节，沉淀各版本发布记录。
